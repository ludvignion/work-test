[project]
name = "daily-work"
version = "0.1.0"
description = "Scripts and analysis for daily work tasks."
requires-python = ">=3.11"
dependencies = [
    "pandas",
    "openpyxl",
    "python-pptx",
    "python-docx",
    "pypdf",
    "python-dotenv",
]

[tool.uv]
package = false
